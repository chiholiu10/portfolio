name: Lint

on:
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Run Lint Checks
        run: |
          echo "🔍 Running lint checks..."
          yarn lint
          echo "✅ Linting passed!"
